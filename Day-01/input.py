a = input("enter the first number: ")
b= input("enter the second  Number: ")
print(a + b)

# this code above takes two numbers but recognizes them as strings.
# so when we add them, it just prints them together instead of adding them.


y = int(input("etnter the 1st number: "))
z = int(input("enter the 2nd number:  "))

c = z + y 
print(c)
#this works perfectly because we changed the input from string to int in order to add them.




n = float(input("enter the 1st number: "))
m = float(input("enter the 2nd number:  "))
g = n + m
print(g)
#this works perfectly because we changed the input from string to float in order to add them.