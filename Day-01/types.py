a = 21
a1 = type(a)
print(a1)

b = 20.12
b1= type(b)
print(b1)

c = "Arslaan is fkine awesome"
c1 = type(c)
print(c1)
#------------------------------------------------------------------------------------------------
#------------------------------------------------------------------------------------------------

z= "20.3"
q = float(z)
m=type(q)
print(m)
#this-float(), converts the string into a float. the float() function is used to convert a string into a float.

#we can also use str() fuction and int() function 
# though we cannot change a string into float or int if the string is not a number. for example,
#  if the string is "hello" then we cannot convert it into a float or int.
#  but if the string is "20.3" then we can convert it into a float and
#  if the string is "20" then we can convert it into an int.